import { default as Screen } from './LoadingScreen';
import { default as Button } from './LoadingButton';
import { default as Spinner } from './LoadingSpinner';

const Loading = {
    Screen,
    Button,
    Spinner,
};

export default Loading;